version: '3.8'

services:
  neo4j:
    image: neo4j:5
    container_name: grove
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/grove2026
    volumes:
      - ./neo4j_data/data:/data
      - ./neo4j_data/logs:/logs